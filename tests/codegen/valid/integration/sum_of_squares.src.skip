fn square(x int) -> int {
    return x * x;
}

fn sum_squares(n int) -> int {
    int sum = 0;
    int i = 1;
    while (i <= n) {
        sum = sum + square(i);
        i = i + 1;
    }
    return sum;
}

fn main() -> int {
    return sum_squares(5);
}
