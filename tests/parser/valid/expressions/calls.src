fn foo() -> int {
    return 42;
}

fn bar(x int, y int) -> int {
    return x + y;
}

fn baz(a int, b float, c bool) -> void {
    return;
}

fn test() -> int {
    int a = foo();
    int b = bar(1, 2);
    int c = bar(bar(1, 2), bar(3, 4));
    baz(10, 3.14, true);
    
    return a + b + c;
}